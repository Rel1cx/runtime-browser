const Settings = async () => ({
  type: "label",
  props: {
    id: "Settings",
    hidden: true,
    textAlign: $align.center,
    text: "not supported yet"
  },
  layout: $layout.fill
})
module.exports = Settings
